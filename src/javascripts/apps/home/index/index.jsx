const React = require('react');
const ReactDOM = require('react-dom');

const LocationSearch = require('./components/LocationSearch');

ReactDOM.render(<LocationSearch />, $('.react-app-home-index').get(0));
