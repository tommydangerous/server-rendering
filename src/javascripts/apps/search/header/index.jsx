const React = require('react');
const ReactDOM = require('react-dom');

const HeaderLocationSearch = require('./components/HeaderLocationSearch');

ReactDOM.render(<HeaderLocationSearch />, $('.react-app-header-search').get(0));
